# 任务 M2-01b：迁移映射补 `admincdn_public`

依赖：M2-01 已合并 `main`。并行组 E。预计 diff ≤ 150 行。基于 `main`，工作分支 `grok/m2-01b`。

## 先读

`docs/dev-plan/README.md` §4 关闭项「3.x `admincdn_public` 未在 §7.2 映射表」；`src/Migration/Mappers.php`（public_assets 相关映射）；`tests/Unit/Migration/FixturesTest.php`；`tests/fixtures/legacy-options/*.json`（看 `admincdn_public` 实际取值：3.8 默认 `["googlefonts"]`，3.9.3 样本为 `[]`）；3.x `Service/Acceleration.php` 里 `admincdn_public` 的合法值列表（只读）。

## 交付物

| 路径 | 改动 |
|------|------|
| `src/Migration/Mappers.php` | `public_assets` = `admincdn_public` 映射 ∪ `admincdn_files` 映射 ∪ `admincdn_dev` 映射，去重、保持 schema 枚举顺序；映射表：`googlefonts→google_fonts`、`googleajax→google_ajax`（若 3.x 取值名不同以 `Service/Acceleration.php` 为准）、`cdnjs`、`jsdelivr`、`emoji`；未知值记 `ignored`（`unsupported_whitelist`）。三个键**都**缺失 → 用 schema 默认；任一存在但合并结果为空 → `[]`（显式清空） |
| `docs/4.0-rewrite-plan.md` §7.2 | 表加一行 `admincdn_public` |
| `tests/Unit/Migration/FixturesTest.php` | 六份 fixture 的 `public_assets` 期望值按新规则更新；新增：只有 `admincdn_public=["googlefonts","cdnjs"]`、files/dev 缺失 → `["google_fonts","cdnjs"]`；三键都缺失 → 默认五项 |

## 禁区

不改 Runner / Backup / Report 结构；不改其它映射；不碰 `src/Admin/app/`。

## 验收

1. `composer check` 全过（migration suite 新增用例）。
2. `grep -n "admincdn_public" src/Migration/Mappers.php docs/4.0-rewrite-plan.md` 各 ≥ 1。
3. `git diff --stat` 只涉及上述三个文件。
4. 提交前缀 `fix(migration): `。
