# 任务 M2-10：修 CI e2e 四条小工具用例（A3 / A5 / A8 / A9），以 CI 为验证回路

worktree 分支 `grok/m2-10`，基于 `main`（`ea37e16`，M2-09 已合）。预计 diff ≤ 300 行。

## 现状

`main` CI run 33995395633 的 `e2e` job：27 条中 23 条通过；`tests/e2e/apps.spec.js` 九条里 A1/A2/A4/A6/A7 过，**A3、A5、A8、A9 失败**。失败日志片段在 `.grok-context/e2e-apps-failures.log`（含 Playwright 的期望/实际/定位器）。已知线索：
- A3：`getByText('已到期')` 严格模式命中 2 个元素（权益表里的状态文字与小工具角标同名）——**测试定位器问题**，用 `getByRole('row')` 范围内定位或 `data-testid`。
- A5：mock 工具 `#log` 只出现 `ready`，没有 `set ok`——`data.set` 在 wp-env 下没走通：查宿主桥 `Bridge.js` 转发 REST 的路径（nonce / REST root / `permissions` 裁剪 / `index_status` 改动是否影响 `apps` 列表里该工具的 permissions）、`AppsController` 对 `PUT /apps/{id}/data/{key}` 的权限判断（mock 工具的 manifest 里 `permissions` 是否含 `data:write`）、以及 wp-env 下 `entry_url`（`http://localhost:8888/wp-content/plugins/wp-china-yes/tests/fixtures/mock-app/index.html`）与宿主 origin **相同**（同源！）——如果桥接把"同源"当成 parent 消息丢弃（M2-09 加了"不理来自 parent 的消息"和 `event.source` 校验），mock 工具在同源 iframe 里发的消息可能被误判。这是最可能的真 bug；修法要保持规格（`event.source === iframe.contentWindow` 校验），而不是放松 origin 检查。
- A8：`index_status: unreachable` 时琥珀提示未渲染——查前端对 `GET /apps` 新响应形状 `{ apps, index_status }` 的处理与 mock "不可达"的方式（e2e 怎么让索引不可达？若靠 filter/常量，wp-env 里是否生效）。
- A9：2 分钟超时——`chromeless.html` 外层 + 内层 sandbox；看是外层页没加载还是等待条件写错。

## 工作方式（本任务特有）

- 本机无 Docker、不能起 wp-env；Studio 站 `wpcy-40` 状态已脏，**不用**。**用 CI 做验证回路**：改完 `git push -u origin grok/m2-10`，用 `gh run list --branch grok/m2-10` / `gh run view <id> --log-failed` 读 e2e 结果，迭代。每轮在报告里记 run id 与结果。**禁止 push 到 `main`。**
- 单元测试（`composer check`）与 `npm run lint:js`、`npm run build` 本地跑。
- 若 A5 根因确实是同源判定：修 `Bridge.js`（保持 `event.source` 严格校验；同源时不能依赖 `event.origin` 区分 parent 与子 iframe，靳靠 `event.source`），补一条单元/合同测试覆盖"同源工具页"场景（`tests/Unit/Apps/BridgeContractTest.php` 或 JS 测试若有）。
- 每条用例只改到通过所需的最小范围；发现真 bug 修在源码，发现测试写错修在测试，报告里分开列。

## 禁区

不改 `Service/`、`client/`、`framework/`；不放松 origin/`event.source` 安全校验来换绿；不删用例；不改无关文件；不 push `main`。

## 验收

1. 分支上 CI `e2e` job 全绿（27/27），报告贴 run id 与逐条结果。
2. `composer check`、`npm run build`、`npm run lint:js` 本地通过。
3. `git diff --stat` 限于 `tests/e2e/`、`src/Admin/app/apps/`、`src/Admin/app/pages/Services.js`、`src/Rest/AppsController.php`、`tests/Unit/Apps/`、`tests/fixtures/mock-app/`。
4. 提交前缀 `test(e2e): ` / `fix(apps): `。
