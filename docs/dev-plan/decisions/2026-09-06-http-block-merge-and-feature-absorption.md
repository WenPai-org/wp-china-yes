# 统筹决定：HTTP Block 并入方案 + 外部插件功能的模块化并入规范

日期：2026-09-06。决定人：feibisi（产品）/ linuxjoy（统筹）。状态：已定稿。

## 一、HTTP Block 并入（feibisi 同意 Grok 分析的方案，见 `verification/http-block-analysis-2026-09-06.md`）

### A1 三层模型

| 层 | 维护者 | 能力 | 边界 |
|----|--------|------|------|
| **L0 受保护主机** | 文派签名下发（`protected_hosts`，并入现有 Ed25519 ruleset）+ 客户端硬编码兜底 | 永远放行 | 用户规则命中无效；多站点不允许站点覆盖 |
| **L1 数据驻留表** | 文派签名下发（现有 A/B/C） | A 改道（跟 profile 闸）/ B 记录 / C 忽略 | 用户不可编辑；不可当拦截器 |
| **L2 本站拦截清单** | 用户（可选，仅网络管理员/单站管理员） | 只能 block | 仅 `exact` / `suffix` 主机匹配；上限 **20** 条；无正则、无通配、无路径；不能改道；不能放行 L0/L1 |

优先级：L0 放行 → L1 驻留 → L2 拦截 → 默认放行；后层不得推翻前层。

### A2 保护主机命中的表现

界面/REST 保存时**拒绝并提示**"文派服务不可拦截"；运行时对已存数据再过滤一层（静默忽略），防绕过 UI。

### A3 受保护清单内容（基线硬编码 ∪ 签名增量）

`*.wenpai.net`（含 `license.` / `api.` / `downloads.` / `updates.` / `ts.`）、`*.wpcy.com`、`cravatar.cn`、`cravatar.com`、`*.admincdn.com`、云桥 ingest 主机。匹配语义与驻留表一致（`exact` / `suffix`）。

### A4 免费 / 高级

**不设高级档。** L0 / L1 / 噪声拦截包走服务端签名下发；L2 免费且极窄；以下**不做**：正则/通配/路径用户规则、全局屏蔽一切外部请求、完整 URL + 来源回溯日志、独立请求日志表与 cron、导入导出拦截规则、第三方规则订阅、智能推荐、仪表盘拉黑小工具、独立 REST 命名空间。

### A5 文派噪声拦截包

签名下发的 block 条（许可心跳、已知无用 API；**不含** .org / CDN / 文派主机），用户只能整包开关，与 NoticeControl 广告规则同一下发机制。

### A6 落地位置与命名

L0 并进 `Privacy/DataResidency/Ruleset`（新字段）；L2 为 `Privacy/SiteBlocklist` 模块，挂在数据驻留旁；**不新设 `HttpBlock` 品牌**。诊断页只读展示三层 + "测一条 URL 会不会被拦"。原插件源码不迁移（从未发布），内置 100+ 黑名单整体丢弃。

## 二、外部插件功能的模块化并入规范（feibisi：后续还会并入多个过往插件，不再单独维护）

### B1 原则

1. **并入的是能力，不是代码。** 先做只读分析（功能清单 / 缺陷 / 与 4.0 重叠 / 落点），统筹拍板后按 4.0 模块合同重写；原插件源码不进仓。
2. **一个能力 = 一个模块**，遵守 `docs/dev/module-authoring.md` 合同（`Module` / `ConditionalModule`，`register()` 只挂钩子、`enabled()` 读 Config），可独立开关、可独立删除。
3. **模块只通过四个"插槽"接入产品**，不得直写别的模块：配置（`Schema` 命名空间 `modules.<name>`）、REST（`wpcy/v1/<name>/*`，同一命名空间下的子路径，不另起命名空间）、设置界面（向"简单/高级"两档各注册若干行，由 M-UI 定义的注册接口）、诊断（向诊断页注册一张只读卡或一段记录）。需要时再加：迁移映射（向 `Migration\Mappers` 注册旧 option 键 → 新键）、服务端规则（向签名 ruleset 增加键，不新增规则类型）。
4. **场景与作用域是横切关注点**：模块声明每个开关在三种场景下的默认值与允许的作用域；不在模块内自行判断"是不是国内站"。
5. **商业露出统一走服务端规则**：模块不带自己的推广位、不写死"了解 →"链接；需要露出的服务登记到服务端下发的服务目录。
6. **用户可见字符串**登记 §4 词表并编号；错误文案按 `copy-guidelines.md`。
7. **删除路径**：每个并入模块的任务书必须写"如何整体移除"（配置键、REST、cron、option 清理），保证以后能再拆出去。

### B2 并入流程（固定四步，每步一个 Grok 任务 + 统筹审）

1. **分析**（只读）：按 HTTP-BLOCK 任务的提纲（功能清单 / 架构质量 / 与 4.0 重叠映射 / 约束冲突 / 免费高级切分 / 工作量）。
2. **决定**：统筹写决定文件（本文件格式）。
3. **规格**：ADR + `config-schema.md` / `rest-api.md` / 词表增量 + 任务书（含 DoD）。
4. **实现**：引擎任务（CI 回路 + 独立审查）；UI 部分进设计门禁随 M-UI 排期。

### B3 已知待并入队列（feibisi 提及"过往开发过的插件"，名单待他给）

| 插件 | 状态 |
|------|------|
| HTTP Block | 分析完成，本决定 A 节 |
| （待补） | |

## 三、任务拆分

- **M-ABSORB-0（文档）**：ADR-005 "外部插件功能的模块化并入规范"（B 节）；`module-authoring.md` 增补"四个插槽"与删除路径；`config-schema.md` / `rest-api.md` / `data-residency-ruleset.md` 增量（`protected_hosts`、`modules.site_blocklist`、噪声包键）；任务书 `M-BLOCK-1.md`（引擎）与 `M-BLOCK-UI.md`（需求说明，进设计门禁）。
- **M-BLOCK-1（引擎）**：L0 + L2 + 噪声包 + 诊断 REST + 测试；依赖 M-SCOPE-1 合入。
- **M-BLOCK-UI**：随 M-UI；设计已在原型 E 的"后台体验"卡预留位置（高级模式下多一行"本站拦截清单"，诊断页多一张"出站请求三层"只读卡）。

## 四、不做什么

不迁移 HTTP Block 源码；不给用户正则/任意域名入口；不设付费墙；不新起品牌名；不在模块内写死场景判断或推广链接。
