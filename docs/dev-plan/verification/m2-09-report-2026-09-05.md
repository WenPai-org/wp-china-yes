先读任务书指定的审查文档和上下文，再按 G 节范围改，不自行扩范围。G 节范围已明确。先装依赖，同时读相关源码和现有测试。依赖已装好。开始按 G 节改绑定确认、权益降级、NoticeControl、expired 和相关建议项。不能往 Studio 站点里 rsync `--delete`。改为把 worktree 同步到 wenpai 仓外路径再软链，A4 夹具拷进 uploads。M2-09 审查修复已提交到 `grok/m2-09`（3 个 commit）。禁区路径无改动。`composer check`、`npm run lint:js`、`npm run build` 已过。e2e A1/A2 在 Studio 上曾单独通过；全量九条后因站点「无权访问后台页」未再全绿。

## 1. 改了哪些文件

`git diff --stat HEAD~3..HEAD`：

```
 composer.json                                      |   1 +
 docs/specs/apps-manifest-and-bridge.md             |   2 +-
 docs/specs/config-schema.md                        |   2 +-
 docs/specs/rest-api.md                             |   6 +-
 src/Admin/AdminModule.php                          |  44 +-
 src/Admin/NoticeControl/NoticeControlModule.php    | 112 ++++-
 src/Admin/app/pages/Connect.js                     |  16 +-
 src/Admin/app/pages/Services.js                    |  28 +-
 src/Apps/Index.php                                 |  50 ++-
 src/Apps/Registry.php                              |   9 +
 src/Config/Schema.php                              |   2 +-
 src/Core/Plugin.php                                |   5 +-
 src/Integrations/Windfonts/Catalog.php             |   1 +
 src/Integrations/Windfonts/Stylesheet.php          |  21 +-
 src/Integrations/Windfonts/WindfontsModule.php     |   8 +-
 src/Migration/Mappers.php                          |   6 -
 src/Rest/AppsController.php                        |  16 +-
 src/Rest/RestModule.php                            |   2 -
 src/Services/Entitlements/EntitlementsModule.php   |  42 ++
 src/Services/SiteBinding/SiteBindingModule.php     | 151 ++++++-
 tests/Unit/Admin/NoticeControlTest.php             |  82 ++++
 tests/Unit/Apps/DataIsolationTest.php              |  26 +-
 tests/Unit/Apps/ExpiredEntitlements.php            |  43 ++
 tests/Unit/Apps/VerifyTest.php                     |   5 +-
 tests/Unit/Core/PluginCreateTest.php               |   2 +
 tests/Unit/Integrations/HookStore.php              |  16 +-
 tests/Unit/Integrations/Windfonts/CatalogTest.php  |   2 +
 tests/Unit/Integrations/Windfonts/StylesheetTest.php |  52 +++
 tests/Unit/Integrations/wp-windfonts-stubs.php     |  16 +-
 tests/Unit/Migration/FixturesTest.php              |  17 +
 tests/Unit/Rest/AdminModuleTest.php                |   4 +-
 tests/Unit/Services/Entitlements/StateMachineTest.php |  57 +++
 tests/Unit/Services/Entitlements/wp-entitlements-stubs.php |  24 +
 tests/Unit/Services/SiteBinding/BindingStore.php   |   8 +
 tests/Unit/Services/SiteBinding/ChallengeFlowTest.php |  66 ++-
 tests/Unit/Services/SiteBinding/wp-binding-stubs.php |  53 +++
 tests/e2e/apps.spec.js                             | 497 +++++++++++++++++++++
 37 files changed, 1438 insertions(+), 56 deletions(-)
```

`git diff --stat HEAD~3..HEAD -- Service/ client/ framework/` 为空。

## 2. 验收命令与输出摘要

### 2.1 `composer check` / JS

- `composer check`：exit 0（phpcs + phpstan + 全量 unit + `test:legacy`）。
- phpstan：`[OK] No errors`（73/73）。
- `test:legacy` 末行：`---- 29 passed, 0 failed ----` / `All PHP syntax and standalone tests passed.`
- `npm run lint:js`：exit 0（仅 ESLint v10 eslintrc 弃用警告）。
- `npm run build`：exit 0，`webpack 5.110.3 compiled with 1 warning`（既有 vendor 体积警告 `907.js` 1.86 MiB）。

### 2.2 新增单测（提交后复跑）

```
vendor/bin/phpunit --testsuite site-binding --filter 'test_start_confirms_in_same_request_when_ready|test_start_pending_retries_via_cron_then_binds|test_confirm_retries_mark_failed'
OK (3 tests, 25 assertions)

vendor/bin/phpunit --testsuite entitlements --filter 'test_entitlement_allows_false_when_exhausted|test_plugin_create_exhausted'
OK (2 tests, 11 assertions)

vendor/bin/phpunit --testsuite admin --filter 'test_generic_notice_warning|test_update_nag_never'
OK (2 tests, 9 assertions)

vendor/bin/phpunit --testsuite apps --filter 'test_expired_write_is_entitlement_required'
OK (1 test, 3 assertions)
```

同套件全量也曾绿：site-binding 12、entitlements 11、admin 19、apps 54。

### 2.3 e2e A1–A9

目标：wenpai Studio `wpcy-40`（`BASE_URL=http://127.0.0.1:8891` 隧道）。A4 `entry_url` 用 uploads 拷贝：

`http://localhost:8891/wp-content/uploads/wpcy-mock-app/index.html`

（wp-env 路径仍写在 `tests/e2e/apps.spec.js` 的 `fixtureEntryUrl()`：`http://localhost:8888/wp-content/plugins/wp-china-yes/tests/fixtures/mock-app/index.html`。）

曾通过：

```
✓ A1: 未绑定打开文派服务 (23.7s)
✓ A2: mock 绑定成功 (29.3s)   # -g 'A2'，rest_route 拦截修好后
  1 passed (59.5s)
```

之后改密码 / 拷插件进站点目录，全量九条多次卡在 `openAdminPage` 等 `#wpcy-admin-root`，页面为 WordPress「Sorry, you are not allowed to access this page.」。`connect.spec.js` E3 同样失败。CLI 侧 `WPCY_KERNEL=v4`、插件 active、`Plugin::create()` 模块列表正常。九条用例在 `tests/e2e/apps.spec.js`，全量未再绿。

### 2.4 `process-timeout`

```
grep -n "process-timeout" composer.json
40:		"process-timeout": 600,
```

## 3. 没做 / 做不到 / 有疑问

- **e2e 九条全绿**：A1、A2 在 Studio 上单独通过；全量被后台页「无权访问」挡住，不是断言写错。未在本机起 wp-env/Docker。
- **4.0 PHPUnit 进 PHP 7.4 job**、**AdminModuleTest 禁词扫描**：任务书明确不做。
- **绑定 `failed` 写入 schema / rest-api**：任务要求 10 次后标 `failed`；不改正文 enum 会被 Validator 打回 `unbound`。补了 `failed`，产品语义仍是「可重新发起」。
- **`GET /apps` 从数组改为 `{ apps, index_status }`**：任务允许补字段；前端兼容旧数组。
- **A4 fixture**：Studio Native PHP 读不到站点外 symlink 的 HTTP 文件，夹具拷到 `wp-content/uploads/wpcy-mock-app/`。
- **diff 量**：含测试约 +1438/−56，超过「预计 ≤ 900」；业务 PHP 主体在预计附近。
- **Studio 站点**：wenpai `wpcy-40` 插件目录现为从 worktree `cp -a` 的实文件（安全钩子禁止对该路径 `rsync --delete` / `rm -rf`），未还原成 symlink。

## 4. 提交哈希

```
d507682 test(e2e): add A1–A9 apps.spec.js for binding, entitlements, and sandbox
faa5341 fix(admin): NoticeControl class whitelist, Windfonts toggle, bridge context
e82ca00 fix(services): confirm binding in-request, wire entitlements, index_status
d689b26 docs(plan): archive M2 independent review; add M2-09 fix task (5 blockers)
96678ef docs(verify): archive Plugin Check triage (23 findings → exclusions with reasons / code fixes / header fixes)
```
